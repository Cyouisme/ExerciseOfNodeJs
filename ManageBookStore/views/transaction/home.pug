extends ../layout/common 

block content
  h1 HELLO, MY NAME IS TRANSACTION!!!
  button(class="btn btn-outline-warning")
    a(href="/transactions/create") CreatesTrans
  table(class="table ml-2")
    thead
      tr
        th UserId
        th BookId
        th Complete
    tbody
      each tran in transactions
        tr
          if user.isAdmin === true
            if tran.iscomplete=== false
              td= tran.userId
              td= tran.bookId
              td 
                a.btn.btn-outline-success(href="/transactions/"+tran.id+"/complete") Complete
            else if tran.iscomplete=== true
          else
            if user.id === tran.userId  
              if tran.iscomplete=== false
                  td= tran.userId
                  td= tran.bookId
                  td 
                    a.btn.btn-outline-success(href="/transactions/"+tran.id+"/complete") Complete
              else if tran.iscomplete=== true
          
