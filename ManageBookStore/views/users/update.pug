extends ../layout/common 

block content
  h1 Hello, My name is UpdatesUser!!!
  if errors   
    each err in errors
      .alert.alert-danger= err
  button(class="btn btn-outline-warning ml-2")
    a(href="/users") HOME
  form(method="POST" id="1")
    .form-group
      label(for="name") New Name: 
      input#name.form-control(type="text",name='name',value=valued ? valued.name:"")
    .form-group
      label(for="phone") New Phone: 
      input#phone.form-control(type="number",name='phone',value=valued ? valued.phone:"")
    button.btn.btn-danger Update
  script.
    var a= document.getElementById("1");
    a.action= "/users/update" + id;